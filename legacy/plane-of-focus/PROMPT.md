# 复刻提示词：焦平面实验台

这是一份把本项目「拆解还原」出来的设计与实现规格，可以整段丢给一个具备浏览器工具（能截图、能起本地服务器调试）的 AI 编码助手，用来从零重建一个同类项目，或者用来改造出自己的变体。

> 说明：本仓库最初的构思参考了 Ryan Sael 的 [*The Plane of Focus*](https://sael.net/plane-of-focus/)（sael.net）——一个同样用可拆解镜头 + 微缩场景讲解焦平面的页面。下面这份提示词描述的是**本仓库自己的实现方案**（独立编写的代码、数值、美术风格），不是对原站源码的转录。如果你打算公开分享复刻结果，请同时保留这一层出处说明。

---

## 0. 一句话目标

做一个单页 3D 科普工具：一只可以拆开的镜头 + 一座微缩场景，让用户通过拖动对焦环/光圈，亲眼看到"焦平面"在空间中的位置，以及光圈如何改变清晰区的厚度——用可交互的三维演示替代教科书插图。

## 1. 核心概念（必须讲对的物理）

- **焦平面（plane of focus）**：镜头在当前对焦距离下，唯一能把光线汇聚成「一个点」的那个空间平面。它垂直于光轴，随对焦距离前后移动。
- **弥散圆（circle of confusion）**：不在焦平面上的点，光线穿过镜头整个孔径后，无法汇聚成一点，会在像平面上摊开成一个小圆盘。圆盘越大，看起来越糊。
- **景深 / 清晰区（depth of field）**：弥散圆小到肉眼分辨不出的范围，前后两个边界之间的距离。
- **光圈（aperture, f 值）**：光圈越大（f 值越小，如 f/2），光锥越粗，同样的偏移量对应的弥散圆越大，景深越浅；光圈越小（f 值越大，如 f/16），景深越深。

对应公式（薄透镜近似，单位统一到 mm 后再换算）：

```
超焦距 H = f² / (N · c) + f
近点   Dn = s(H − f) / (H + s − 2f)
远点   Df = s(H − f) / (H − s)          // s ≥ H 时远点为无穷远

给定物距 d、对焦距 s、光圈 N、焦距 f，某点在像平面上的弥散圆直径：
coc = f² / (N(s − f)) · |d − s| / d
```

其中 f=焦距(mm)，N=光圈值，c=容许弥散圆直径(mm)，s=对焦距离(mm)，d=物体距离(mm)。

本项目取值：f = 50mm（全画幅标准镜头），传感器 36×24mm，容许弥散圆 c = 0.03mm，对焦范围 25–95cm，光圈范围 f/2–f/16。**所有"清晰/模糊"的判断和数值显示都必须走这套公式**，不能用假的视觉近似糊弄。

## 2. 场景叙事（美术方向）

不要用真实照片写实风格，用**低多边形（low-poly）微缩模型**风格：

- 一座木质托盘上的迷你山谷：草坪（带一条土路）、若干簇低多边形松树、一座亮着暖光窗户的小木屋、路灯、背景是几座低多边形雪山，再往后是一块弧形画布画的天空（云朵用椭圆堆叠出来，地平线有远山剪影）。
- 场景里选 3 个"被摄物"作为教学锚点，分布在不同距离上（例如近处一棵树、中距离小屋、远处山峰），景深解说文案要点名这三者中哪些清晰、哪些模糊。
- 镜头本身走"实验室精密仪器"路线：黑色阳极氧化镜筒 + 铜色齿轮环 + 玻璃镜片用物理正确的透射材质（`transmission`/`ior`），前组/对焦组/光圈/后组分成独立部件，可以沿光轴"拆开"到互相分离、露出内部。
- 镜头后面立一块磨砂玻璃（像平面），实时显示这只镜头"拍"到的画面：必须是真实渲染（离屏相机 + 景深后处理），且**上下颠倒**（真实光学成像特性），不能用假图片糊弄。
- 整个装置放在一张光具座（optical bench）上，带厘米刻度；下方是一个控制台，台面上有仪表化的 UI（拨盘、滑轨、按钮），风格类比"摄影棚监视器台"。
- 背景是一个摄影棚/工作室房间：地砖地板、墙板、挂在墙上的几张"从糊到清晰"对比灯箱、绿植、置物架，让镜头装置有一个可信的物理环境，而不是浮在纯色背景里。

配色：深色背景（接近 `#0b0f1a`），青色（`#7fe3ff`）作为"清晰/焦平面/交互高亮"的主强调色，琥珀色（`#ffb454`）作为"模糊/警示"的对比色，暖橙做灯光点缀。玻璃材质、镜头镀膜、UI 高亮统一用青色语言，让用户一眼认出"这是可交互/这是焦平面相关"的元素。

## 3. 3D 实现要点（Three.js）

- **不用外部模型文件**，所有几何体用代码参数化生成：`LatheGeometry` 转出镜筒/镜片剖面、`ExtrudeGeometry` 挤出货真价实的齿轮齿形、`InstancedMesh` 批量摆树/灌木/石头、`CanvasTexture` 手绘木纹/岩石/刻度尺/天空贴图。地形用 `PlaneGeometry` + 顶点位移函数（噪声 + 到小路距离）生成起伏，按坡度/是否靠近小路上色。
- **光轴系统**：约定一个世界坐标轴（比如 +X 轴）为光轴，所有光学量（对焦距离、超焦距、近远点）都是这根轴上的一维坐标，方便公式和 3D 位置互相换算。
- **光圈虹膜**：不要用简单缩放的圆代替，用 N 片叶片（例如 9 片）真实的多边形虹膜几何，半径随光圈值变化、叶片带旋转量，看起来像真实相机光圈。
- **"焦平面"可视化**：一个跟随对焦距离在光轴上滑动的半透明发光网格平面，大小随视场角撑满当前距离的画幅；叠加一个从近点到远点的半透明"景深方盒"，让用户直观看到这层"清晰区"的厚度。
- **光线可视化**：从每个被摄物向镜头前组发出一束采样光线，穿过前组→光圈→后组，最终打在像平面上；清晰的物体光线收束成一点（青色），模糊的物体在像平面上画出一个圆盘（琥珀色，大小=真实计算出的弥散圆）。可以用一个自定义着色器（`onBeforeCompile` 注入）给场景里的材质统一叠加一条"焦平面扫过就发光"的效果，强化"这一层是清晰的"视觉反馈。
- **拆开/组装动画**：给镜头每个功能组（前组/光圈/对焦组/后组/卡口）各自一个"沿光轴的分离偏移量"，用一个 0→1 的过渡参数插值；同时配合一个裁剪平面（`clippingPlanes`）在拆开状态下切掉朝向摄像机一侧的外壳，露出剖面。
- **实时"照片"渲染**：单独起一个沿光轴看向场景的透视相机，渲染到离屏 RenderTarget（要带深度纹理）；用一个全屏后处理 pass 按每像素深度算弥散圆半径，做一个简单的可变半径散景模糊（螺旋采样即可，不需要真实光线追踪）；这张结果贴到磨砂玻璃网格上，`uv` 要整体翻转模拟倒像。只有对焦距离或光圈变化时才重新渲染这张图，不要每帧都渲染，省性能。
- **相机控制**：自己实现一个轻量的轨道/平移/缩放/第一人称漫游控制器（不依赖 `OrbitControls` 也可以，图的是自由度）：左键拖拽=轨道旋转，右键/Shift+左键=平移，滚轮/双指捏合=缩放，WASD=水平位移，QE=转向。所有相机目标值都做成"current 值阻尼追赶 goal 值"的形式（指数阻尼 `1-exp(-rate*dt)`），保证任何切换都是平滑过渡而不是瞬移。
- **"电影镜头"自动演示模式**：预先编排 6–8 个机位（起止位置+朝向+持续时间+期间联动的对焦/光圈变化），无人操作时循环播放，营造"广告级运镜"的效果；用户一旦有任何拖拽/滚轮/按键操作，立刻退出自动模式并把当前插值位置作为新的自由视角起点，不能有跳变。

## 4. 交互与 UI 规格

页面四角布局：

- **左上**：标题 + 一句话说明 + （可选）"N 人正在看"活跃人数 + 三个读数卡片（对焦距离 / 光圈值 / 清晰区厚度）+ 一段会根据当前状态动态改写的解说文字（明确点出哪个物体清晰、哪个模糊、为什么）。
- **右上**：控制面板——对焦预设三档（近/中/远）分段按钮、一条可拖拽的对焦距离滑杆（帶预设刻度）、光圈三档分段按钮（大光圈/中/小，展示成 f 值）、镜头"组装/拆开"切换、"电影镜头"开关图标按钮、"?"说明按钮。
- **场景内**：给关键部件（像平面、对焦环、焦平面、清晰区、拆开后的四大组件）挂 3D 定位的 HTML 标签（每帧用 `Vector3.project(camera)` 投影到屏幕坐标），标签要能感知遮挡（比如被磨砂玻璃挡住时隐藏或半透明）。
- **说明弹窗**：分 5-6 段依次讲清楚"什么是焦平面 / 为什么其余部分会糊 / 对焦环怎么工作 / 光圈怎么工作 / 为什么像是倒的"，末尾附快捷键表。
- **快捷键**：`1/2/3` 切三档对焦、`[`/`]` 微调对焦（Shift 加大步长）、`F` 循环光圈档位、`X` 切换拆开/组装、`C` 切换电影镜头、`R` 重置视角、`/` 隐藏全部 UI 只看画面、`WASD` 移动、`QE` 转向、拖拽/滚轮走鼠标手势。
- **控制台里的实体 UI**：除了 DOM 悬浮面板，场景内的"控制台"本身也要有可点击的三维物件——比如一排显示 5 个不同对焦距离画面的小屏幕（点击直接跳到那个对焦距离）、一个可拖拽的物理滑块/旋钮、几个光圈材质按钮。做法是用 `Raycaster` 拾取场景里带 `userData.pick` 标记的网格，指针按下/拖动/松开时分别处理"拖滑块/点按钮/拖对焦环"等不同手势。
- **响应式**：窄屏（~760px 以下）把右侧控制面板收到屏幕底部、改成横向可换行布局；隐藏非必要的说明文字，保留核心读数；触屏下双指缩放、单指拖拽轨道。
- **无障碍/降级**：`prefers-reduced-motion: reduce` 时默认关闭电影镜头自动播放；所有可交互控件要有 `aria-pressed`/`aria-label`；弹窗要能 `Escape` 关闭、有 `role="dialog"`。

## 5. 文案与语气

- 用第二人称、口语化但准确的中文科普语气，不要堆砌术语；专有名词第一次出现给通俗解释（比如"弥散圆"要接一句"光斑"帮读者建立直觉）。
- 解说文字要**联动实时状态**动态生成，不是一段固定文案——根据"当前有几个物体清晰/模糊"分支出至少三种句式（全清晰 / 全糊 / 部分清晰部分糊，并点名具体物体）。
- UI 标签尽量短（两三个字的中文词：对焦、光圈、镜头、组装、拆开），用等宽字体展示数值（厘米、f 值），保证对齐和"仪器感"。

## 6. 技术约束 / 工程规格

- **不依赖构建工具链**：Three.js 通过 `<script type="importmap">` 从 CDN（如 jsDelivr）引入 ES Module，直接 `import * as THREE from 'three'`；页面整体是可以单文件分发的静态 HTML + 内联 `<script type="module">`。
- 源码按"职责"拆成多个小文件（光学公式/场景/镜头/光具座与控制台/景深渲染/相机控制/UI 交互/主循环各一个文件），用一个几十行的小脚本（哪种语言都行，Python/Node 均可）按顺序拼接成最终单文件产物，方便开发时分文件维护、发布时零依赖分发。
- **性能预算**：检测 `matchMedia('(pointer: coarse)')` 区分触屏设备，触屏上降低像素比上限和离屏渲染分辨率；离屏"照片"渲染只在对焦距离/光圈实际变化时才重新渲染，不要每帧都渲染一次全场景。
- 深色配色写成 CSS 变量，预留浅色/无障碍扩展空间；所有尺寸支持从手机宽度到桌面宽度。
- 页面要有清晰的错误兜底：如果 WebGL/模块加载失败，展示一段友好的中文提示，而不是白屏。

## 7. 验收清单

复刻完成后，至少要能验证这些行为，且不需要看源码只靠操作页面就能验证：

- [ ] 拖动对焦环/滑块，焦平面网格在场景里跟着前后移动，三个读数（对焦距离/光圈/清晰区）随之刷新且数值合理（换算成超焦距公式能对上）。
- [ ] 切换光圈档位，清晰区厚度明显变化（f/16 比 f/2 深很多），像平面上的画面景深模糊程度同步变化。
- [ ] 磨砂玻璃上的画面是"真的在渲染"这个 3D 场景（不是贴图），且是上下颠倒的。
- [ ] 点击"拆开"，镜头四大组件沿光轴分离、露出内部结构和光路；再点"组装"能无缝合回去。
- [ ] 三个预设距离按钮、控制台上的 5 格胶片点击、拖拽物理滑块/旋钮，效果一致（殊途同归改的是同一个状态）。
- [ ] 全部列出的快捷键都生效，说明弹窗内容和实际快捷键对得上。
- [ ] 无操作一段时间后自动进入"电影镜头"巡游；一旦用户拖拽/滚轮/按键立刻退场，视角不跳变。
- [ ] 移动端宽度下布局不破版，双指能缩放、单指能拖拽轨道。
- [ ] 断网或 WebGL 不可用时，页面给出可读的中文错误提示而不是白屏或原生报错弹窗。

---

<sub>本文件由 <a href="https://claude.com/claude-code">Claude Code</a> 基于本仓库的实际实现整理而成。</sub>
